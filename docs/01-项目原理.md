# 项目原理

> 本文解释"为什么要这么做"。**怎么装**见 [02-部署-通用.md](02-部署-通用.md) 与
> [03-部署-AX3000-自编内核模块.md](03-部署-AX3000-自编内核模块.md)，**怎么用**见 [04-使用方法.md](04-使用方法.md)。

---

## 0. 术语表

| 词 | 在本项目里的含义 |
| --- | --- |
| **出口** | 一次 PPPoE 拨号所获得的接入通道。校园网每次拨号可能落到不同的出口设备/策略组，质量不一样。 |
| **好出口 / 坏出口** | 好出口能正常访问你要用的服务；坏出口会对**部分服务**限速（表现为请求超时、加载不出来），但基础连通性还在。 |
| **会话（session）** | 一条独立的 PPPoE 连接。本项目的连接池就是"同一账号的多条会话"。 |
| **连接池（pool）** | 由多条会话组成的聚合出口，由 mwan3 按连接分摊流量。 |
| **desync** | 主动扰乱 TLS ClientHello 的发送方式，使链路中间的 DPI 读不到（或读错）SNI 域名。 |
| **hostlist** | desync 的域名名单。**留空 = 对所有域名生效**（实测语义，见 §7.4）。 |
| **探针（probe）** | 用来判断"当前出口好不好"的在线请求。 |
| **nfqws / tpws** | zapret 的两种工作模式：前者走内核 NFQUEUE（只把每连接前几个包送进用户态），后者是用户态透明代理（全部载荷过用户态）。 |

---

## 1. 问题：拨号出口的好坏是随机的

校园网通常用 PPPoE 接入。账号认证通过后，BRAS（本项目实测是华为 **ME60-X8A**）会把这次会话
分配到某个出口。实测现象：

- 好出口：一切正常；
- 坏出口：**基础连通性没问题**（ping 通、能打开部分网站），但对某些服务（实测中最典型的是
  一部分腾讯系服务）会被限速甚至直接不可用，表现为"某个应用就是加载不出来"。

对用户来说，最省事的应对不是去研究限速规则，而是**换一个出口**：断开、重新拨号，直到落在一个好出口上。
这就是本项目两部分功能的第一块——**出口检测 + 自动重拨**。

第二块是**带宽**：单条 PPPoE 会话的持续下行有一个上限（本项目实测在 **40~100 Mbps** 量级波动，
取决于时段与测速方式），而 BRAS **允许同一个账号建立多条会话**，于是多条会话 + 按连接分流
可以近似线性地叠加带宽（实测 **1.9× ~ 3×**）。这就是**多拨连接池**。

第三块是**对抗按 SNI 的限速**：坏出口的限速并不是"整个 IP 段全限"，而是按 TLS 握手里的
**SNI 域名**做黑名单。于是可以让 DPI 读不到 SNI，从而落回"不限速"的默认类别。这就是
**SNI 分流（desync）**。

---

## 2. 检测：怎么判断"这个出口好不好"

思路是**用真实业务做探针**，而不是看链路层指标（链路层在坏出口上也是通的，看不出问题）。

- **普通模式**：对一组探针地址做多轮 HTTP(S) 请求。每轮内多次请求**全部成功**才算这一轮通过；
  **连续三轮**（且中间有递增的等待间隔）都通过，才判定为"好出口"。这样做的原因是坏出口的表现
  往往是"时通时断"或"慢到超时"，单次成功不足以说明问题。
- **200 Mbps 模式**：直接从测速端点下载一段时间，按实际速率判断是否超过阈值（默认 150 Mbps）。
  它针对的是"出口没被限速但带宽不达标"的情况。
  > **在路由器上做这个测量有三个坑，必须避开**（详见 [05-故障排查.md](05-故障排查.md) §5.9）：
  > ① 测速流量必须**按会话打 mwan3 的 fwmark** 钉住（只绑源地址会被主路由表带走，测的是别的会话）；
  > ② 用**明文 HTTP**（HTTPS 由路由器自己跑 TLS 会先撞 CPU 上限，200M 通道会被读成 100M）；
  > ③ **多流 + 先暖机再计量**（单流填不满被整形的通道：实测同一条会话单流 15.9、6 流 212.1 Mbps）。
- 两种模式可以**同时启用**：两者都通过才算好出口，任一失败就重新拨号再测一遍。

判定通过后，程序/守护进程按策略停下（Windows 侧默认成功后退出，路由器侧进入 `success` 状态并
持续维护连接池）。

> **探针地址必须换成自己学校可达的地址。** 仓库里默认的探针（斗鱼 CDN 接口）与测速端点
> （西电的 `test.xidian.edu.cn`）只是实测环境的示例，在别的网络里很可能不可达。

---

## 3. 重拨状态机

两侧实现（Windows 的 C# 程序 / 路由器的 `campus-rediald`）逻辑一致，都是：

```
空闲 ──启动──▶ 拨号 ──拨通──▶ 静置等待 ──▶ 探测/测速 ──通过──▶ 成功（停止或保持）
                   ▲                              │
                   └────────── 失败且未达上限 ──────┘
                              （失败后等待一段时间再拨）
                   达到最大尝试次数 ──▶ 失败（停在 failed，不再无限重拨）
```

几个刻意的设计：

- **拨号后先静置再探测**：PPPoE 刚起来的一两秒里网络尚未可用，立刻探测会产生假失败。
- **失败重拨有间隔**：BRAS 对同一账号的两次认证有最小间隔限制（实测报错
  `E2532: The two authentication interval cannot be less than 3 seconds`），拨得太快只会连续认证失败。
- **有上限**：达到 `max_attempts` 就停在失败态，避免"永远在重拨"把用户网络搞得不可用。
- **忙时让路**：会话出口的秒级流量超过 `busy_threshold_kbytes` 时推迟检测，避免在用户正在下载时
  把会话拆掉。
- **只动自己该动的东西**：路由器侧只操作配置的 WAN 接口与本包自己创建的 `wan2..wan6` 派生接口，
  从不重启整个网络、不碰 LAN / 桥 / 无线 / LuCI / uhttpd。

---

## 4. 多拨连接池：为什么"同账号多会话"能成立

### 4.1 关键实测结论

| 结论 | 实测依据 |
| --- | --- |
| 同一账号**允许多路并发**，前提是**源 MAC 不同** | 用临时 macvlan + 手工 `pppd` 起第二条会话：**4 秒拨通**，拿到**独立 IP**，主会话全程不受影响 |
| **同 MAC 多会话被禁止** | 第二条会话一出现，BRAS 立刻发 `PADT` 把原会话踢掉 |
| **VLAN 多拨不通** | 带 802.1Q 标签的 PADI 到不了 BRAS（`PADO` 永远超时），即使把 VLAN id 置 0 也会被插 4 字节标签后被丢弃 |
| 所以每条会话都要"独立 MAC + 不带标签的以太帧" | 这正是 macvlan（`mode bridge`，挂在物理 WAN 口上）的作用 |

### 4.2 实现方式

- 第 2 条会话起，用 **macvlan** 在物理 WAN 设备上创建独立 MAC 的下层设备
  （接口名用短名 `crwanN`，因为 Linux 接口名上限 15 字符），每条会话一个独立 MAC + 独立 `pppname`；
- 会话由 netifd 管理（普通 `wanN` 段），由本项目的守护进程串行冷启动拨号（拨一条、等它稳、再拨下一条）；
- **池大小由账号决定**：所有启用账号的 `max_connections` 之和 = `pool_size`；
- **认证间隔用 60 秒**：实测 5 秒间隔只能收敛 2/5 条，60 秒可收敛 5/5。

### 4.3 分流：mwan3

把池里的会话注册进 mwan3（`wan`/`wan2`/… 各作一个 member），并用一条**本项目自己的**策略规则
（按连接加权分摊）抢在出厂规则之前生效。

一个实测踩到的坑：mwan3 给每个接口建"每接口路由表"时，数据源是 `ip route list table main`；
而 **netifd 只会给"主"PPPoE 装默认路由**，`wan2/wan3` 的默认路由从来没进 main 表，
于是 mwan3 判这些接口 `error (16)` 并把它们排除出策略——**看起来在池里，实际所有流量回落到一条会话**。
修法是让每条会话的默认路由直接进它自己的路由表（`network.wanN.ip4table`），或给它 `defaultroute 1`。
详见 [05-故障排查.md](05-故障排查.md)。

---

## 5. 带宽：为什么能叠加、能叠多少

单条会话与聚合的持续下行（2026-09-11 实测；PC 经路由器转发、明文 HTTP 多流、路由器侧读
`pppoe-wanN` 的 `rx_bytes` 增量，并用 PC 网卡计数交叉验证）：

| 场景 | 实测 |
| --- | --- |
| 单个 TCP 流（落在一条 200 Mbps 通道上） | **212 ~ 244 Mbps** —— 一条流就能吃满一条通道 |
| 2 条流（都被 mwan3 随机分到同一条会话） | 204 Mbps —— 与单流相同，另外 4 条会话完全浪费 |
| 10 条流（未开快路径） | **339 ~ 354 Mbps** |
| 10 / 20 / 40 条流（开启快路径后） | **621 ~ 696 / 688 ~ 707 / 717 Mbps** |

### 5.1 真正的共享瓶颈是路由器的 CPU（softirq），不是校园网

未开快路径时，10 条流就能把 CPU 打到 **99.5%**（`cpu1` 全程 100%，其中 softirq ~92%），
而总吞吐只有 354 Mbps——同期 3 条会话的速率分别只有 14 / 57 / 115 Mbps，**远低于它们各自
~200 Mbps 的通道上限**。上限既不是某条会话、也不是测速服务器，而是"**每个包都要走一遍
netfilter 慢路径**"的 CPU：PPPoE 解封装 + conntrack + mwan3 mark + NAT + NFQUEUE 钩子 + br-lan 转发，
全部落在单核 softirq 上。

对照：同一台测速服务器单流直连（RTT 1.5 ms）能跑 **244 Mbps**，所以"校园网只能给 350 Mbps"
是错的结论——**是这台路由器转不动**。

**"浮动"从哪来**：mwan3 的 `cr_balanced` 是**按连接随机**（每条连接 20% 概率落到某条会话），
连接数少时分配很不均——实测同样 10 条流，两次跑的逐会话分布可以是
`77.7/155/155.5/155.2/77.7`（合计 621）也可以是 `37.5/197.9/132.3/130.3/111.7`（合计 609）。
连接数越多越接近均值，但单次测速的读数本来就会跳（300/400/500 就是这一条 + CPU 饱和共同作用）。

### 5.2 解法：flow offload（转发快路径）+ 首包保护

内核里 `xt_FLOWOFFLOAD` / `nf_flow_table` / `nf_flow_table_hw` 本来就编进去了（本项目自编内核
模块时已确认），只是**从来没有规则去用它**（`xt_FLOWOFFLOAD` 引用计数 0）。补上之后：

| | 未开快路径 | 开启快路径 |
| --- | --- | --- |
| 10 条流 | 339 ~ 354 Mbps | **621 ~ 696 Mbps** |
| 20 / 40 条流 | CPU 已满，上不去 | **688 ~ 707 / 717 Mbps** |
| CPU 峰值（softirq） | 99.5%（92%） | ~70%（64%） |
| HTTPS 单流（带 desync） | 244 Mbps | 243 Mbps（无回归） |
| SNI 混淆是否仍生效 | 是 | 是（NFQUEUE 命中 ~4.5 包/连接） |

**关键：不能只插一条 `-j FLOWOFFLOAD`**。裸规则会让 TCP 在三次握手完成时就进入快路径，
此后 nfqws 再也看不到 ClientHello（实测整个测试期间 NFQUEUE 命中数 **0**），SNI 混淆**静默失效**。
正确形状是三条，把 80/443 的前 8 个**原始方向包**留在慢路径：

```sh
# 1) 80/443：连接发出 >= 8 个原始方向包（握手 + ClientHello 之后）才注册快路径
iptables -t filter -I FORWARD 1 -p tcp -m multiport --dports 80,443 \
  -m connbytes ! --connbytes 1:8 --connbytes-mode packets --connbytes-dir original \
  -j FLOWOFFLOAD
# 2) 其它 TCP 立即进快路径
iptables -t filter -I FORWARD 2 -p tcp -m multiport ! --dports 80,443 -j FLOWOFFLOAD
# 3) 非 TCP（UDP/QUIC 等）立即进快路径；UDP 首包仍走慢路径，nfqws 的 UDP 混淆不受影响
iptables -t filter -I FORWARD 3 ! -p tcp -j FLOWOFFLOAD
```

这正好回答"**能不能让下行不做任何处理以保持带宽**"：**能**。快路径让下行的载荷完全不进
netfilter，而混淆只作用于握手头几个包；两者不冲突，前提是**别让快路径过早接管**。
落地脚本：`CampusRedialWrt/tools/install-flow-offload.sh`（含幂等的 fw3 include 注册，
防火墙 reload / 重启后自动补回）。

> ⚠️ **测量方法本身是个大坑**：如果让路由器自己跑 TLS 去测（例如在路由器上 `curl` 12 路 HTTPS），
> 瓶颈会先落在**路由器的 CPU**上（实测只有 ~79 Mbps），根本测不到链路或代理的上限。
> 正确做法是让**别的机器**做 TLS，路由器只转发。详见 [05-故障排查.md](05-故障排查.md) 的"测量方法学"。

---

## 6. SNI 分流：对抗按域名的限速

### 6.1 被对抗的是什么

链路中间的 DPI 设备会解析 TLS ClientHello，读出其中的 **SNI（域名）**，对黑名单里的域名限速。
注意它限的是**域名**，不是 IP——所以同一个 IP 上的不同域名待遇可以不同。

### 6.2 对抗思路

让 DPI **读不到或读错** SNI，使这条连接落回默认（不限速）类别。工程上常用的几种手法：

| 手法 | 做法 |
| --- | --- |
| **split / multisplit** | 把 ClientHello 拆成多个 TCP 段发送，让只按单包解析的 DPI 看不到完整 SNI |
| **disorder** | 打乱分段的发送顺序，DPI 需要重组才能读到 SNI |
| **fake** | 先发一个"假的" ClientHello（伪造的 SNI / 非法校验），让 DPI 记住错误的域名，真正的握手随后进行 |

本项目用上游工具 **zapret** 实现（`nfqws` 或 `tpws` 两种模式），配置里对 80/443 分别指定策略。
这些手法只作用于**握手的头几个包**，不碰后续数据——这正是 `nfqws` 高效的原因。

### 6.3 两种模式的关键差别（本项目实测）

| | **nfqws** | **tpws** |
| --- | --- | --- |
| 实现位置 | 内核 NFQUEUE（`connbytes` 限制只把**每连接前几个包**送进用户态） | 用户态**透明代理**（`nat` 跳转到 `127.0.0.127:988`） |
| 数据面 | 载荷完全留在内核快路径 | **两个方向**的载荷都要过一遍用户态进程 |
| 需要内核支持 | 需要 NFQUEUE（`xt_NFQUEUE` + `nfnetlink_queue`） | 只需要 `nat` 表，几乎所有固件都有 |
| 本项目实测（转发路径，16 路明文 HTTP） | **312.5 / 314.7 Mbps** | **173.1 / 180.4 Mbps** |
| 对照：完全不经手 | 355.1 Mbps | — |

> 表中数字是**未开启转发快路径**时测的，此时"完全不经手"也只有 355 Mbps——因为瓶颈已经在
> 路由器 CPU 上（见 §5.1）。开启快路径（`tools/install-flow-offload.sh`）后，同一装置下
> nfqws 的聚合是 **621 ~ 707 Mbps**，CPU 峰值从 99.5% 降到 ~70%：**nfqws 本身不再是主要成本，
> "快路径有没有开"才是**。

值得特别说明的一点：**"上行不受影响"不代表下行没被处理**。tpws 是代理，两个方向都要过用户态；
上行看起来无损，只是因为**上行线速率（250 Mbps）低于 tpws 的处理天花板**，而下行高于它。
（早期文档里"上行是内核直通、只有首包走 tpws"的说法是错的，已在
[reference/SNI-DESYNC.md](reference/SNI-DESYNC.md) 与工作日志中更正。）

### 6.4 hostlist 的语义（反直觉，必须记住）

- `--hostlist=<文件>` 表示"**只处理名单里的域名**"；
- 但**文件存在却是空的**（0 字节）在实测中等于"**匹配所有主机**"（v72.13，debug 日志里
  profile 标注 `(empty)`，且不做任何 hostlist 检查就直接命中）。
- 所以：**"名单是空的" ≠ "分流没生效"**，而是"对全部域名做 desync"。本机就是故意留空的用法。
- 反过来，想让"大流量站点不限速地跑"而只保护少数域名，应该用**排除集**（`nozapret`，
  DNAT 规则本来就用 `-m set ! --match-set nozapret dst`）或改成"只让名单目标进代理"的白名单模式。

### 6.5 适用范围：只管 80/443（含 UDP 443），**不管游戏**

`nfqws` 的过滤条件（本机 `/opt/zapret/config` 与 mangle 规则一致，实测口径）：

| 方向 | 匹配 | 作用 |
| --- | --- | --- |
| 上行 | `--filter-tcp=80` / `--filter-tcp=443` / `--filter-udp=443`（`connbytes 1:6`） | 拆/伪造 ClientHello、HTTP 请求头、QUIC Initial |
| 下行 | `--sports 80,443`（`--connbytes-dir reply 1:3`） | 让 nfqws 看到服务端最早的 3 个回包（部分 desync 模式需要） |

**不在这张表里的流量，一个包都不会进 NFQUEUE** —— 所以"打游戏时 SNI 分流好像没起作用"
不是故障，是**设计使然**：

- 游戏几乎都是**自定义 UDP/TCP 协议**（典型端口 27015–27050、3074、3478–3479、500/4500，
  或游戏自选高位端口），包里既没有 TLS ClientHello 也没有 HTTP Host ⇒ **没有 SNI 可混淆**，
  也就没有任何"解"可言；
- 校园网那类 DPI 的"按域名限速"本身只针对能被域名识别的流量（网页/视频/下载）。游戏的问题是
  端口/IP 级 QoS、UDP 限速丢包、NAT 会话数或纯粹的延迟抖动——**不是同一类问题，SNI 分流治不了**；
- 一句话：**SNI 分流治"按域名限速"，不治"游戏卡"**。

反过来有两处**确实会被这套配置碰到**，需要注意：

1. **UDP 443 会被当成 QUIC 处理**（`--filter-udp=443 --dpi-desync=fake --dpi-desync-repeats=6`）。
   有些游戏/工具故意用 UDP 443 穿防火墙，它们每个流的头几个包会被复制成多份假包
   （实测该规则一直在命中：5 条会话分别 13/12/10/21/17 包）。若某游戏正好用 UDP 443 且表现异常，
   用下面的排除集处理。
2. **hostlist 为空 = 全量 desync**（本机就是这种用法），于是游戏的登录/更新/语音信令/反作弊这些
   **走 443 的 TLS 流量也在被 desync**，用的是较激进的 `--dpi-desync=fake,multidisorder` +
   `--dpi-desync-split-pos=1,midsld` + `--dpi-desync-fooling=badseq,md5sig`。绝大多数站点无所谓，
   但个别服务器/中间盒会 reset 或卡住 ⇒ 表现成"登录转圈/更新卡住/进不去房间"，看起来像分流在捣乱。
   需要的可以不用排除集，而是把 hostlist 从"空"改成**只写真正需要解限速的域名**。

**排除办法（按 IP，可持久）**：NFQUEUE 规则里的 `! --match-set nozapret` 就是为此留的口子 ——
把 IP/网段逐行写进 `/opt/zapret/ipset/zapret-ip-exclude.txt`（zapret 启动时灌进 `nozapret` 集合），
重启 `zapret` 生效；临时验证可直接 `ipset add nozapret <ip>`（重启后丢失，适合先试）。
实测（`iptables -t mangle -Z` 清零计数器后，各 5 条 HTTPS 到同一 IP）：

| | 上行命中 | 下行命中 |
| --- | --- | --- |
| 未排除 | **45 包** | **34 包** |
| 已排除 | 8 包（约 0.5 s 窗口内的后台噪声） | **0 包** |

> 另外有一件**与分流无关、却常被算在它头上**的事：**多拨本身**。mwan3 是**按连接**随机钉会话
> （见 §4.3），游戏的一条流只会走一条会话（**不叠加**，还可能落在劣化会话上），登录流与游戏流
> 甚至可能走不同公网 IP ⇒ NAT 类型/组队体验变差；守护进程重拨低于阈值的会话时，那条会话上的
> 游戏连接会**立刻断**。要"游戏稳定优先"，应当把游戏目标钉到固定会话并避开巡检重拨，
> 而不是指望 desync。

---

## 7. 为什么路由器上还需要"自编内核模块"

通用 OpenWrt 设备只要内核带 NFQUEUE 就能直接用 nfqws。但本项目实测的这台 Redmi AX3000
用的是**社区自编的 QSDK 供应商内核**：

- 官方 OpenWrt 从来没有 ipq50xx 这个 target，core feed 404，**装不上任何 target 级 kmod**；
- 借用别的 target 的同架构 kmod 也不行（`vermagic` 与 PLT 段不匹配）；
- 更狠的是：**`vermagic` 相同也不代表 ABI 相同**——QSDK 改过 `struct net_device`（本例大 64 字节），
  用上游头文件编出来的模块 `insmod` 会成功，但一碰结构体就 panic。

所以要么放弃这些内核模块，要么**用厂商自己的内核源码树 + 设备原装 config 自己编**。
本项目走的是后者，并配了一套**离线 ABI 闸门**（配置保真比对、编译期 `offsetof` 表、
符号对照）来保证"能编出来"真的等于"能装上去不炸"。

具体做法、踩过的坑与验收记录见 [03-部署-AX3000-自编内核模块.md](03-部署-AX3000-自编内核模块.md)，
深度技术细节见 [reference/AX3000-KMOD-BUILD.md](reference/AX3000-KMOD-BUILD.md)。

---

## 8. 组件与数据流

```
┌─ Windows 侧（仓库根）────────────────────────────┐
│  rasdial 断/连 ──▶ 探针/测速 ──▶ 判定 ──▶ 重拨    │  单机使用；只恢复用户已有的拨号连接
│  C# 控制台 / PowerShell 脚本 / 托盘通知           │
└──────────────────────────────────────────────────┘

┌─ OpenWrt 侧（CampusRedialWrt/）──────────────────────────────────────────┐
│                                                                          │
│  LuCI 页面 ──▶ rpcd 插件 ──▶ 控制文件 ──▶ campus-rediald（POSIX shell）  │
│                    │                          │                          │
│                    │                          ├─ 拨号/重拨（netifd, uci）│
│                    │                          ├─ 探测 / 测速            │
│                    │                          ├─ macvlan 多会话 + mwan3 │
│                    │                          └─ 状态/统计/日志落盘      │
│                    │                                                     │
│                    └─▶ SNI 分流面板 ──▶ /etc/init.d/zapret               │
│                              ├─ nfqws（内核 NFQUEUE，首选）              │
│                              └─ tpws（用户态代理，回退）                  │
│                                                                          │
│  自编内核模块（kmod-build/）：macvlan / xt_statistic / NFQUEUE 两件套     │
└──────────────────────────────────────────────────────────────────────────┘
                    │                                    │
              LAN 客户端 ────────────────────────▶ WAN（多路 PPPoE）──▶ 校园网
```

---

## 9. 设计边界（本项目**不**做的事）

- 不绕过认证、不伪造凭据、不修改白名单、不探寻未授权带宽通道；
- 只恢复/维护**用户自己已有**的拨号连接，只操作配置中指定的 WAN 接口与本包自己创建的派生接口；
- 凭据只写入设备上的 UCI 配置文件（权限 `0600`），**不会**出现在状态 RPC、日志或前端返回值里；
- 探针与测速端点默认值是示例，正式部署请替换为自己或学校明确授权的健康检查地址。

---

## 10. 相关文档

| 文档 | 内容 |
| --- | --- |
| [02-部署-通用.md](02-部署-通用.md) | 任意 OpenWrt 设备的部署 |
| [03-部署-AX3000-自编内核模块.md](03-部署-AX3000-自编内核模块.md) | 本机（Redmi AX3000）的特殊改动与自编内核模块 |
| [04-使用方法.md](04-使用方法.md) | 装好之后怎么用 |
| [05-故障排查.md](05-故障排查.md) | 按现象查原因、给修法 |
| [reference/AX3000-KMOD-BUILD.md](reference/AX3000-KMOD-BUILD.md) | 自编内核模块的深度技术记录 |
| [reference/SNI-DESYNC.md](reference/SNI-DESYNC.md) | SNI 分流的部署与验收指南（多版本 OpenWrt） |
